/**
 * History shows the same records two ways. The URL vocabulary for them is
 * owned by shared routing; this module only names its own type after it.
 */
export { HISTORY_VIEWS } from "@/shared/routing/app-router";
export type { HistoryView as HistoryViewMode } from "@/shared/routing/app-router";

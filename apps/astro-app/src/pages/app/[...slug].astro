---
import { AppShell } from "@/core/modules/app/shell"
import AppLayout from "@/core/layouts/layout.astro"

export const prerender = true

export const getStaticPaths = () => {
  return [{ params: { slug: undefined } }]
}
---

<AppLayout>
  <AppShell client:load />
</AppLayout>
